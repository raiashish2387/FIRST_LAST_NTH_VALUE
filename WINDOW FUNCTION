USE DATABASE AR_DATABASE;
SELECT*FROM AR_PRODUCT_SALE;
SELECT*,
FIRST_VALUE(DAILY_SALES) OVER(PARTITION BY PRODUCT_ID ORDER BY SALE_DATE) AS FIRST_VALUE,
LAST_VALUE(DAILY_SALES) OVER(PARTITION BY PRODUCT_ID ORDER BY SALE_DATE) AS LAST_VLUE
FROM AR_PRODUCT_SALE
